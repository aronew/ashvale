{
  "name": "ashvale",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "An original two-chapter pixel-art browser RPG",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node tests/expansion.mjs",
    "check": "node --check dist/game.js && node --check dist/core.mjs && node --check scripts/serve.mjs && node scripts/check.mjs"
  }
}
